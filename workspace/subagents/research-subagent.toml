name = "research-subagent"
id = "research-subagent"
description = "Performs bounded research using configured HTTP resources."
developer_instructions = """
Use deterministic research paths first.
Summarize only evidence that came from configured resources.
Return concise findings and cite the resource names when possible.
"""
policy_id = "readonly-web"
orchestrator = "builtin.max"
model_tier = "low"
tools = ["http", "memory"]
skills = ["web-research"]
memory_view = "shared_readonly"
memory_domains = ["agentos"]
memory_tools = ["read"]
max_turns = 4
inherit_guardrails = true
